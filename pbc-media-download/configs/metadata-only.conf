# Metadata extraction only - no download
# Usage: yt-dlp --config-location configs/metadata-only.conf <url>

# Skip download
--skip-download

# Write full metadata JSON
--write-info-json

# Output location for JSON file
--output /c/Users/drewa/pbcs/pbc-media-download/downloads/%(title)s.%(ext)s
